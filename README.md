# ZMP

ZMP is a simple music player written in the [Zig](https://ziglang.org/) programming language. It uses the [miniaudio.h](https://miniaud.io/) library for processing and playing audio files. The TUI is created using the [libvaxis](https://github.com/rockorager/libvaxis) library.
